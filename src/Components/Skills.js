function Skills(props) {
    const list = props.skills.map((skill) =>
        <div>
                // {skill}
        </div>
    )
    return (
        <div>
            {list}
        </div>
    ) }

export default Skills